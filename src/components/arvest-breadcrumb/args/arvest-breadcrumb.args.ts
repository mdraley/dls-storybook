import { getDescription } from '../../../global/ts/globalDocs';

const webcomponent = 'arvest-breadcrumb',

  arvestBreadcrumb = {
    'links': {
      'description': getDescription(
        webcomponent,
        'links'
      ),
      'control': 'text'
    }
  };

export default arvestBreadcrumb;
