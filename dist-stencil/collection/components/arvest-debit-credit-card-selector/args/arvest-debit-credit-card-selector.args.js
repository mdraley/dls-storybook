import { getDescription } from '../../../global/ts/globalDocs';
const webcomponent = 'arvest-debit-credit-card-selector', arvestDebitCreditCardSelectorArgs = {
  linkcta: {
    description: getDescription(webcomponent, 'linkcta'),
    control: 'text'
  },
  label: {
    description: getDescription(webcomponent, 'label'),
    control: 'text'
  },
  selectButton: {
    description: getDescription(webcomponent, 'selectButton'),
    control: 'text'
  },
  showLink: {
    description: getDescription(webcomponent, 'showLink'),
    control: 'boolean'
  },
  showLabel: {
    description: getDescription(webcomponent, 'showLabel'),
    control: 'boolean'
  },
  showButton: {
    description: getDescription(webcomponent, 'showButton'),
    control: 'boolean'
  },
  bgimage: {
    description: getDescription(webcomponent, 'bgimage'),
    control: 'text'
  }
};
export default arvestDebitCreditCardSelectorArgs;
